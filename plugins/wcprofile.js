const fs = require('fs'); const path = require('path'); const { readEnv } = require('../lib/database'); const { cmd, commands } = require('../command'); async function sendReplies(conn, from, replies, pushname) { for (const [index, reply] of replies.entries()) { setTimeout(async () => { await conn.sendMessage(from, { text: reply.replace('${pushname}', pushname) }, { quoted: null }); }, index * 700); } } cmd({ on: "body" }, async (conn, mek, m, { from, body, isOwner, pushname }) => { const sequenceTrigger = "sv"; if (body.toLowerCase() === sequenceTrigger.toLowerCase()) { const replies = [`> ඔයව සේව් 🫢💖 ${pushname}`, '*I am Hashan🐼💖 👤*', '*From - Matara 🇱🇰 📍*', '*Age - 21+ 🎂*', '*මාවත් සේව් දාගන්න💀💖 📩*', '*Thank You...😏✌️*']; await sendReplies(conn, from, replies, pushname); } if (body.toLowerCase().startsWith('name')) { const nameReply = `*Your Name Is* ${pushname}`; await conn.sendMessage(from, { text: nameReply }, { quoted: null }); } });
